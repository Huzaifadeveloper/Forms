var player = "kohli"
var scored = "100"
var team =  "pakistan"
var trophy = "T20worldcup2022"

var out = "Virat "+player+" scored "+scored+" against "+team+" in "+trophy+"."

var out = Virat '${player}" scored "${scored}" against "${team}" in "${trophy}